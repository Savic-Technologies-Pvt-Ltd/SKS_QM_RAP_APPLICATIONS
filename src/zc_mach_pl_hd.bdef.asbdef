projection;
strict ( 2 );
use draft;

define behavior for ZC_MACH_PL_HD //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action getpdf;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
use action getItem;
  use association _Item { create; with draft; }
}

define behavior for ZC_MACH_PL_IT //alias <alias_name>
{
  use update;
  use delete;

  use association _HDR;
}