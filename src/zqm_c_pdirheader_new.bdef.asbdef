projection;
strict ( 2 );
use draft;

define behavior for ZQM_C_PDIRHeader_NEW //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
use action getpdf;
  use association _Item {  with draft; }
}

define behavior for ZQM_C_PDIRITEM_NEW //alias <alias_name>
{
  use update;
  //use delete;

  use association _HDR;
}