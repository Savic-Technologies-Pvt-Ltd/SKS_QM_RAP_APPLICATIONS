projection;
strict ( 2 );
use draft;

define behavior for ZQM_CM_ILOT_H //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Discard;
  use action Resume;
  use action Activate;
  use action  Prepare;

  use association _Item { create; with draft; }

}


define behavior for ZQM_CM_ILOT_I //alias <alias_name>
{
  use update;
  use delete;

  use association _Header { with draft; }
}