projection;
strict ( 2 );
use draft;
use side effects;
define behavior for ZCDS_QM_INS_LOT_P01 //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action getpdf;
  use association _Item { create; with draft; }

}

define behavior for ZCDS_QM_INS_LOT_P02 //alias <alias_name>
{
  use update;
  use delete;

  use association _Header { with draft; }
}